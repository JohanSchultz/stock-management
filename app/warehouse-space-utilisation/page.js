import { AppShell } from "@/components/AppShell";
import { WarehouseSpaceUtilisation } from "./WarehouseSpaceUtilisation";

export default function WarehouseSpaceUtilisationPage() {
  return (
    <AppShell title="Warehouse Space Utilisation">
      <WarehouseSpaceUtilisation />
    </AppShell>
  );
}
